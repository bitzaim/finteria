export type TradeOptions = {
  title: string;
  text: string;
  subTitle: string;
};

export type SmallOptions = {
  text: string;
  icon: string;
};

export type OlItems = {
  title: string;
  text: string;
};
